{
  "name": "mstrmnd-layer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MSTRMND Layer — interactive WebGPU visualization of the agentic operating layer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "check": "vgpu check src/layer.wgsl",
    "snapshot": "node scripts/snapshot.mjs"
  },
  "dependencies": {
    "vgpu": "^0.3.1"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
